import React from "react"

const Blockcard = (properties) => {
    console.log(properties)
    return(
        <div className = "Blockcard" >
        <h3>Blog title</h3>
        <p>Blog description</p>
    </div>
    )
}

export default Blockcard;